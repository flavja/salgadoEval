{% extends 'base.html.twig' %}

{% block title %}{{parent()}} - Accueil {% endblock %}

{% block content %}
    <div class="row">
    <div class="col">
        <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
            <ol class="carousel-indicators">
                <li data-target="#carouselExampleIndicators" data-slide-to="1" class="active"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="3"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="4"></li>
            </ol>
            <div class="carousel-inner">
                {% for result in shuffledResult %}
                    {% set active = result == shuffledResult |first ? "active" : "" %}

                <div class="carousel-item {{active}}">
                    <img class="d-block w-100" src="{{result.image}}" alt="slide">
                    <div class="carousel-caption d-none d-md-block">
                        <h5>{{result.name}}</h5>
                        <p>{{result.description}}</p>
                    </div>
                </div>
                {% endfor %}
            </div>
            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Précédent</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Suivant</span>
            </a>
        </div>
    </div>
    </div>
    <div class="row" style="margin: 10px;">
        <div class="col">
            <img src="https://www.lemonde.fr/blog/lunettesrouges/files/2013/05/IMG_5446-530x438.jpg" class="rounded float-left" alt="Tito">
            <p>Sur cette photo, nous pouvons apercevoir le célèbre Tito Salgado, propriétaire de ce site et des oeuvres qui y sont affichées</p>

        </div>
    </div>
{% endblock %}
{% block javascript %}
<script>
    $('.carousel').carousel();
</script>

{% endblock %}

